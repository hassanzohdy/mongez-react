import { trans, extend, translateFrom } from "./translator";
import { currentDirection, directionOf, getCurrentLocaleCode, getLocaleCodes, updateCurrentLocaleCode } from "./locales";
export { trans, directionOf, translateFrom, getLocaleCodes, currentDirection, getCurrentLocaleCode, updateCurrentLocaleCode, };
declare const _default: {
    extend: typeof extend;
};
export default _default;
