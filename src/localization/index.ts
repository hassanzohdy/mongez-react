export type { LocaleCodes } from './types';
export { getLangMode, getLocalizedText } from './utils';
export { default, trans, extend, translateFrom } from "./translator";
export { currentDirection, directionOf, getCurrentLocaleCode, getLocaleCodes, updateCurrentLocaleCode } from "./locales";

