import PropTypes from 'prop-types';
declare function Theme(props: any): JSX.Element;
declare namespace Theme {
    var propTypes: {
        children: PropTypes.Validator<any>;
    };
}
export default Theme;
