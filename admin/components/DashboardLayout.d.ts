import PropTypes from 'prop-types';
declare function DashboardLayout(props: any): JSX.Element;
declare namespace DashboardLayout {
    var propTypes: {
        children: PropTypes.Validator<any>;
    };
}
export default DashboardLayout;
